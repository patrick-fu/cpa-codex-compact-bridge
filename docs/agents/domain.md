# Domain Docs

This is a single-context repository.

Before exploring implementation work, read the root `CONTEXT.md` and relevant ADRs in `docs/adr/` when they exist. Their absence is not an error.

`CONTEXT.md` is a glossary only: use it for stable domain vocabulary, not implementation plans. Create ADRs only for hard-to-reverse decisions with meaningful trade-offs.
